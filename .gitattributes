android/llmkit/src/main/jniLibs/**/*.so filter=lfs diff=lfs merge=lfs -text
# Prebuilt iOS llama.xcframework (binaries + dSYMs) — built by build-llama-xcframework.yml
Frameworks/** filter=lfs diff=lfs merge=lfs -text
